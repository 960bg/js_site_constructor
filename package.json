{
  "name": "js_site_constructor",
  "source": "./src/index.html",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "parcel --open",
    "build": "parcel build --dist-dir ./dist/",
    "start": "parcel serve --public-url / --dist-dir dist",
    "predeploy": "rm -rf dist && parcel build ./src/index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "parcel": "^2.10.2"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.2",
    "jquery": "^3.7.1",
    "popper.js": "^1.16.1"
  }
}